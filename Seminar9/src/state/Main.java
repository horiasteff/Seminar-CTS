package state;

public class Main {
    public static void main(String[] args) {

        ComandaMancare comanda = new ComandaMancare(1);
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
        comanda.nextState();
    }
}
